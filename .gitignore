node_modules/
*.js.map
.DS_Store
Thumbs.db

# Vault-local plugin runtime data — never part of the plugin source or a release.
# Shipping these would overwrite a user's settings/layouts on update.
data.json
roll-history.json
backups/

# The built bundle is attached to GitHub Releases by the release workflow and
# is committed here for manual / BRAT installs. Uncomment to keep it out of
# source control instead.
# main.js

# FreeFileSync sync databases (created by the vault/project sync)
*.ffs_db

# Word vectors the user supplies, and the table built from them: large, local,
# and reproducible - they belong in the plugin folder, not in the repository.
glove.txt
semantic-en.json
