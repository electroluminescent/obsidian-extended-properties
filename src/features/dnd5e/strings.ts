/** D&D 5e-module locale strings - data in `strings.json` (F4: i18n as data). */
import type { StringDict } from "../../i18n/i18n";
import data from "./strings.json";

export const dndEn = data as StringDict;
