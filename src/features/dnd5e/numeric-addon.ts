/**
 * Removed: superseded by the core modifier addon
 * (`ui/render/modifier-addon.ts`) and the rolling module's addon
 * (`features/rolling/numeric-addon.ts`). This stale copy predated the
 * module split and was no longer registered anywhere — it also no longer
 * compiled against the current `rules.ts`.
 */

export {};
