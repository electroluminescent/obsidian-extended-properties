/**
 * Removed: superseded by the rolling module's dice-pool roll service
 * (`features/rolling/roll-service.ts`). This stale copy predated the module
 * split and was no longer registered anywhere — it also no longer compiled
 * against the current `rules.ts`.
 */

export {};
