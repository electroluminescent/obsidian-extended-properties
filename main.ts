/**
 * Removed in v2.2: this was the stale v1 single-file plugin source. It was
 * not part of the build (esbuild bundles `src/main.ts` → `main.js`) and not
 * typechecked (tsconfig includes `src/**` only); it predated the modular
 * architecture described in ARCHITECTURE.md. The real entry point is
 * `src/main.ts`.
 */

export {};
